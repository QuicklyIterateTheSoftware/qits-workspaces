# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service test -Dtest=OpenApiSchemaExportTest` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    AgentActivityState:
      type: string
      enum:
      - IDLE
      - BUSY
      - WAITING
      - ENDED
    ApiError:
      type: object
      description: "The error envelope: a message, plus a reason on a 409."
      properties:
        message:
          type: string
          description: "What went wrong, in words. Always present."
        reason:
          $ref: "#/components/schemas/Reason"
          type: string
          description: "Which refusal this is. Present on the 409s the release and\
            \ integrate flows raise, absent on every other error. A value a client\
            \ does not recognise is shown as the message verbatim."
        conflicts:
          type: array
          items:
            type: string
          description: The conflicted paths. Present only for CONFLICT and MERGE_CONFLICT.
    CleanupBranchRequest:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
        result:
          type: string
    CreateWorkspaceRequest:
      type: object
      required:
      - repositoryId
      - id
      properties:
        repositoryId:
          type: string
          pattern: \S
        id:
          type: string
          pattern: \S
        parent:
          type: string
        branch:
          type: string
        preamble:
          type: string
        adoptExisting:
          type: boolean
    DiscardWorkspaceRequest:
      type: object
      properties:
        result:
          type: string
    Entry:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Entry1:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDto"
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    IntegrateRequest:
      type: object
      required:
      - summary
      properties:
        summary:
          type: string
          maxLength: 100
          pattern: \S
    MergeBranchRequest:
      type: object
      required:
      - source
      properties:
        source:
          type: string
          pattern: \S
        target:
          type: string
        result:
          type: string
    MergeWorkspaceRequest:
      type: object
      properties:
        target:
          type: string
    Reason:
      type: string
      enum:
      - CONFLICT
      - MERGE_CONFLICT
      - NOT_FAST_FORWARD
      - ALREADY_INTEGRATED
      - PUSH_REJECTED
      - VERSION_ALREADY_RELEASED
      - RELEASE_REQUIRED
    ReleaseBranchRequest:
      type: object
      required:
      - branch
      - summary
      properties:
        branch:
          type: string
          pattern: \S
        summary:
          type: string
          maxLength: 100
          pattern: \S
    ReleaseRequest:
      type: object
      required:
      - summary
      properties:
        summary:
          type: string
          maxLength: 100
          pattern: \S
    Response:
      type: object
      properties:
        version:
          type: string
        commitSha:
          type: string
        branch:
          type: string
    Response1:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
        cleanedUp:
          type: boolean
    Response10:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Response11:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response12:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response13:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry1"
    Response14:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response2:
      type: object
      properties:
        success:
          type: boolean
    Response3:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ServiceEventDto"
    Response4:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    Response5:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
    Response6:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry"
    Response7:
      type: object
      properties:
        commitSha:
          type: string
        branch:
          type: string
        targetBranch:
          type: string
    Response8:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    Response9:
      type: object
      properties:
        success:
          type: boolean
    ServiceEventDto:
      type: object
      properties:
        repoId:
          type: string
        workspaceId:
          type: string
        workspaceRowId:
          type: integer
          format: int64
        serviceId:
          type: string
        serviceName:
          type: string
        kind:
          $ref: "#/components/schemas/ServiceEventKind"
        severity:
          $ref: "#/components/schemas/ServiceEventSeverity"
        status:
          $ref: "#/components/schemas/ServiceStatus"
        summary:
          type: string
        logExcerpt:
          type: string
        commandId:
          type: string
        source:
          type: string
        anchorFrom:
          type: integer
          format: int64
        anchorTo:
          type: integer
          format: int64
        sourceEpoch:
          $ref: "#/components/schemas/Instant"
        timestamp:
          $ref: "#/components/schemas/Instant"
    ServiceEventKind:
      type: string
      enum:
      - STATUS_CHANGED
    ServiceEventSeverity:
      type: string
      enum:
      - INFO
      - WARNING
      - ERROR
    ServiceStatus:
      type: string
      enum:
      - STARTING
      - READY
      - RESTARTING
      - CRASHED
      - STOPPED
    UpdateHistoryRequest:
      type: object
      properties:
        preamble:
          type: string
        result:
          type: string
    WorkspaceCommandDto:
      type: object
      properties:
        id:
          type: string
        actionName:
          type: string
        status:
          type: string
        exitCode:
          type: integer
          format: int32
        launchedAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        branch:
          type: string
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
        conflictsWithParent:
          type: boolean
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        runtimeStatus:
          $ref: "#/components/schemas/WorkspaceRuntimeStatus"
        runtimeError:
          type: string
        clean:
          type: boolean
        agentActivity:
          $ref: "#/components/schemas/AgentActivityState"
        preamble:
          type: string
        result:
          type: string
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        daemonConnectedAt:
          $ref: "#/components/schemas/Instant"
        daemonVersion:
          type: string
        daemonBuildTime:
          $ref: "#/components/schemas/Instant"
        daemonOutdated:
          type: boolean
    WorkspaceEventDto:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/WorkspaceEventType"
        branch:
          type: string
        parent:
          type: string
        target:
          type: string
        commit:
          type: string
        note:
          type: string
        at:
          $ref: "#/components/schemas/Instant"
    WorkspaceEventType:
      type: string
      enum:
      - CREATED
      - MERGED
      - UPDATED_FROM_PARENT
      - INTEGRATED
      - ABANDONED
    WorkspaceHistoryDetailDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        preamble:
          type: string
        result:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        events:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceEventDto"
        commands:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceCommandDto"
    WorkspaceHistoryDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceRuntimeStatus:
      type: string
      enum:
      - RUNNING
      - STOPPED
      - PROVISIONING
      - FAILED
    WorkspaceStatus:
      type: string
      enum:
      - ACTIVE
      - INTEGRATED
      - ABANDONED
paths:
  /workspaces/api/branches/cleanup:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CleanupBranchRequest"
        required: true
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response2"
        "400":
          description: "The branch is not eligible: uncommitted changes, unmerged\
            \ commits, or dependents."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      summary: Cleanup Branch
      tags:
      - Branch Controller
  /workspaces/api/branches/merge:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeBranchRequest"
        required: true
      responses:
        "200":
          description: Merged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response1"
        "400":
          description: "A blank, dash-leading or self-referential branch name."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The target is the repository's default branch. `reason` is
            RELEASE_REQUIRED and the message names the two endpoints that do write
            it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      summary: Merge Branch
      tags:
      - Branch Controller
  /workspaces/api/branches/release:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseBranchRequest"
        required: true
      responses:
        "200":
          description: Released; the version and the merge commit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          description: "A blank, dash-leading or oversized field — or the default\
            \ branch itself, which a release lands on and cannot be released into."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: "No such repository, or the origin has no such branch."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Nothing was released and the default branch is unchanged. `reason`
            says which refusal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      summary: Release Branch
      tags:
      - Branch Controller
  /workspaces/api/history:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response13"
      summary: List
      tags:
      - Workspace History Controller
  /workspaces/api/history/{id}:
    patch:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHistoryRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response12"
      summary: Update
      tags:
      - Workspace History Controller
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response14"
      summary: Get
      tags:
      - Workspace History Controller
  /workspaces/api/service-events:
    get:
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: pageSize
        in: query
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 500
          minimum: 1
      - name: repoId
        in: query
        schema:
          type: string
      - name: severity
        in: query
        schema:
          $ref: "#/components/schemas/ServiceEventSeverity"
      - name: since
        in: query
        schema:
          $ref: "#/components/schemas/Instant"
      - name: source
        in: query
        schema:
          type: string
      - name: workspaceId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response3"
      summary: List
      tags:
      - Service Event Controller
  /workspaces/api/workspaces:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response6"
      summary: List
      tags:
      - Workspace Controller
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response10"
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/active-process:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response11"
      summary: Active Process
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/delete-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
      summary: Delete Container
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/discard:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscardWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response9"
        "400":
          description: Bad Request
      summary: Discard
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/ensure-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response8"
      summary: Ensure Container
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/integrate:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntegrateRequest"
        required: true
      responses:
        "200":
          description: Merged into the parent branch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response7"
        "400":
          description: "No summary, an oversized one, or a workspace with no branch\
            \ to integrate."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: "Nothing landed and the target is unchanged — including when\
            \ the parent is the default branch, which only /release may write. `reason`\
            \ says which refusal."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      summary: Integrate
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/merge:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeWorkspaceRequest"
        required: true
      responses:
        "200":
          description: Merged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response5"
        "409":
          description: The target is the repository's default branch. Use /integrate
            or /release.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "400":
          description: Bad Request
      summary: Merge
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/recreate-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response4"
      summary: Recreate Container
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/release:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseRequest"
        required: true
      responses:
        "200":
          description: Released; the version and the merge commit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          description: "No summary, an oversized one, or a workspace with no branch\
            \ to release."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Nothing was released and the default branch is unchanged. `reason`
            says which refusal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      summary: Release
      tags:
      - Workspace Controller
  /workspaces/api/workspaces/{id}/stop-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
      summary: Stop Container
      tags:
      - Workspace Controller
info:
  title: qits-workspaces API
  version: 1.0.0-SNAPSHOT

# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service test -Dtest=OpenApiSchemaExportTest` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    AgentActivityState:
      type: string
      enum:
      - IDLE
      - BUSY
      - WAITING
      - ENDED
    BootstrapOutcome:
      type: string
      enum:
      - SKIPPED
      - SUCCEEDED
      - FAILED
    BootstrapRunDto:
      type: object
      properties:
        bootstrapCommandId:
          type: string
        commandName:
          type: string
        outcome:
          $ref: "#/components/schemas/BootstrapOutcome"
        commandId:
          type: string
        exitCode:
          type: integer
          format: int32
        ranAt:
          $ref: "#/components/schemas/Instant"
    BootstrapStepDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
    CleanupBranchRequest:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
        result:
          type: string
    CreateWorkspaceRequest:
      type: object
      required:
      - repositoryId
      - id
      properties:
        repositoryId:
          type: string
          pattern: \S
        id:
          type: string
          pattern: \S
        parent:
          type: string
        branch:
          type: string
        preamble:
          type: string
        adoptExisting:
          type: boolean
    DiscardWorkspaceRequest:
      type: object
      properties:
        result:
          type: string
    Entry:
      type: object
      properties:
        step:
          $ref: "#/components/schemas/BootstrapStepDto"
        lastRun:
          $ref: "#/components/schemas/BootstrapRunDto"
    Entry1:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Entry2:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDto"
    Entry3:
      type: object
      properties:
        instance:
          $ref: "#/components/schemas/ServiceInstanceDto"
    HealthCheckDto:
      type: object
      properties:
        name:
          type: string
        kind:
          $ref: "#/components/schemas/HealthCheckKind"
        port:
          type: integer
          format: int32
        path:
          type: string
        expectStatus:
          type: string
        command:
          type: string
        intervalMs:
          type: integer
          format: int64
        timeoutMs:
          type: integer
          format: int64
        healthyThreshold:
          type: integer
          format: int32
        unhealthyThreshold:
          type: integer
          format: int32
        initialDelayMs:
          type: integer
          format: int64
    HealthCheckKind:
      type: string
      enum:
      - HTTP
      - TCP
      - COMMAND
    HealthCheckState:
      type: string
      enum:
      - HEALTHY
      - UNHEALTHY
      - UNKNOWN
    HealthCheckStatusDto:
      type: object
      properties:
        name:
          type: string
        kind:
          $ref: "#/components/schemas/HealthCheckKind"
        state:
          $ref: "#/components/schemas/HealthCheckState"
        lastLatencyMs:
          type: integer
          format: int64
        lastCheckedAt:
          $ref: "#/components/schemas/Instant"
        detail:
          type: string
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    MergeBranchRequest:
      type: object
      required:
      - source
      properties:
        source:
          type: string
          pattern: \S
        target:
          type: string
        result:
          type: string
    MergeWorkspaceRequest:
      type: object
      properties:
        target:
          type: string
    Response:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
        cleanedUp:
          type: boolean
    Response1:
      type: object
      properties:
        success:
          type: boolean
    Response10:
      type: object
      properties:
        success:
          type: boolean
    Response11:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Response12:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response13:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response14:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry2"
    Response15:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response16:
      type: object
      properties:
        instance:
          $ref: "#/components/schemas/ServiceInstanceDto"
    Response17:
      type: object
      properties:
        instance:
          $ref: "#/components/schemas/ServiceInstanceDto"
    Response18:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry3"
    Response2:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ServiceEventDto"
    Response3:
      type: object
      properties:
        started:
          type: boolean
    Response4:
      type: object
      properties:
        started:
          type: boolean
    Response5:
      type: object
      properties:
        chainRunning:
          type: boolean
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry"
    Response6:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    Response7:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
    Response8:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry1"
    Response9:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    RestartPolicy:
      type: string
      enum:
      - NEVER
      - ON_FAILURE
      - ALWAYS
    ServiceDefinitionDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        startScript:
          type: string
        readyPattern:
          type: string
        stopSignal:
          type: string
        restartPolicy:
          $ref: "#/components/schemas/RestartPolicy"
        autoStart:
          type: boolean
        maxRestarts:
          type: integer
          format: int32
        otel:
          type: boolean
        webView:
          $ref: "#/components/schemas/WebViewDto"
        environment:
          type: object
          additionalProperties:
            type: string
        healthChecks:
          type: array
          items:
            $ref: "#/components/schemas/HealthCheckDto"
    ServiceEventDto:
      type: object
      properties:
        repoId:
          type: string
        workspaceId:
          type: string
        workspaceRowId:
          type: integer
          format: int64
        serviceId:
          type: string
        serviceName:
          type: string
        kind:
          $ref: "#/components/schemas/ServiceEventKind"
        severity:
          $ref: "#/components/schemas/ServiceEventSeverity"
        status:
          $ref: "#/components/schemas/ServiceStatus"
        summary:
          type: string
        logExcerpt:
          type: string
        commandId:
          type: string
        source:
          type: string
        anchorFrom:
          type: integer
          format: int64
        anchorTo:
          type: integer
          format: int64
        sourceEpoch:
          $ref: "#/components/schemas/Instant"
        timestamp:
          $ref: "#/components/schemas/Instant"
    ServiceEventKind:
      type: string
      enum:
      - STATUS_CHANGED
    ServiceEventSeverity:
      type: string
      enum:
      - INFO
      - WARNING
      - ERROR
    ServiceInstanceDto:
      type: object
      properties:
        definition:
          $ref: "#/components/schemas/ServiceDefinitionDto"
        status:
          $ref: "#/components/schemas/ServiceStatus"
        restartCount:
          type: integer
          format: int32
        commandId:
          type: string
        proxyPath:
          type: string
        health:
          type: array
          items:
            $ref: "#/components/schemas/HealthCheckStatusDto"
    ServiceStatus:
      type: string
      enum:
      - STARTING
      - READY
      - RESTARTING
      - CRASHED
      - STOPPED
    UpdateHistoryRequest:
      type: object
      properties:
        preamble:
          type: string
        result:
          type: string
    WebViewDto:
      type: object
      properties:
        port:
          type: integer
          format: int32
        entryPath:
          type: string
        basePath:
          type: string
    WorkspaceCommandDto:
      type: object
      properties:
        id:
          type: string
        actionName:
          type: string
        status:
          type: string
        exitCode:
          type: integer
          format: int32
        launchedAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        branch:
          type: string
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
        conflictsWithParent:
          type: boolean
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        runtimeStatus:
          $ref: "#/components/schemas/WorkspaceRuntimeStatus"
        runtimeError:
          type: string
        clean:
          type: boolean
        agentActivity:
          $ref: "#/components/schemas/AgentActivityState"
        preamble:
          type: string
        result:
          type: string
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        daemonConnectedAt:
          $ref: "#/components/schemas/Instant"
        daemonVersion:
          type: string
        daemonBuildTime:
          $ref: "#/components/schemas/Instant"
        daemonOutdated:
          type: boolean
    WorkspaceEventDto:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/WorkspaceEventType"
        branch:
          type: string
        parent:
          type: string
        target:
          type: string
        commit:
          type: string
        note:
          type: string
        at:
          $ref: "#/components/schemas/Instant"
    WorkspaceEventType:
      type: string
      enum:
      - CREATED
      - MERGED
      - UPDATED_FROM_PARENT
      - INTEGRATED
      - ABANDONED
    WorkspaceHistoryDetailDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        preamble:
          type: string
        result:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        events:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceEventDto"
        commands:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceCommandDto"
    WorkspaceHistoryDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceRuntimeStatus:
      type: string
      enum:
      - RUNNING
      - STOPPED
      - PROVISIONING
      - FAILED
    WorkspaceStatus:
      type: string
      enum:
      - ACTIVE
      - INTEGRATED
      - ABANDONED
paths:
  /api/history:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response14"
      summary: List
      tags:
      - Workspace History Controller
  /api/history/{id}:
    patch:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHistoryRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response13"
      summary: Update
      tags:
      - Workspace History Controller
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response15"
      summary: Get
      tags:
      - Workspace History Controller
  /api/repositories/{repoId}/branches/cleanup:
    post:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CleanupBranchRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response1"
        "400":
          description: Bad Request
      summary: Cleanup Branch
      tags:
      - Branch Controller
  /api/repositories/{repoId}/branches/merge:
    post:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeBranchRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          description: Bad Request
      summary: Merge Branch
      tags:
      - Branch Controller
  /api/service-events:
    get:
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: pageSize
        in: query
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 500
          minimum: 1
      - name: repoId
        in: query
        schema:
          type: string
      - name: severity
        in: query
        schema:
          $ref: "#/components/schemas/ServiceEventSeverity"
      - name: since
        in: query
        schema:
          $ref: "#/components/schemas/Instant"
      - name: source
        in: query
        schema:
          type: string
      - name: workspaceId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response2"
      summary: List
      tags:
      - Service Event Controller
  /api/workspaces:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response8"
      summary: List
      tags:
      - Workspace Controller
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response11"
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Workspace Controller
  /api/workspaces/{id}/active-process:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response12"
      summary: Active Process
      tags:
      - Workspace Controller
  /api/workspaces/{id}/bootstrap-commands:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response5"
      summary: List
      tags:
      - Workspace Bootstrap Controller
  /api/workspaces/{id}/bootstrap-commands/run:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response4"
      summary: Run Chain
      tags:
      - Workspace Bootstrap Controller
  /api/workspaces/{id}/bootstrap-commands/{stepId}/run:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: stepId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response3"
      summary: Run Single
      tags:
      - Workspace Bootstrap Controller
  /api/workspaces/{id}/delete-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
      summary: Delete Container
      tags:
      - Workspace Controller
  /api/workspaces/{id}/discard:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscardWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response10"
        "400":
          description: Bad Request
      summary: Discard
      tags:
      - Workspace Controller
  /api/workspaces/{id}/ensure-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response9"
      summary: Ensure Container
      tags:
      - Workspace Controller
  /api/workspaces/{id}/merge:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response7"
        "400":
          description: Bad Request
      summary: Merge
      tags:
      - Workspace Controller
  /api/workspaces/{id}/recreate-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response6"
      summary: Recreate Container
      tags:
      - Workspace Controller
  /api/workspaces/{id}/services:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response18"
      summary: List
      tags:
      - Workspace Service Controller
  /api/workspaces/{id}/services/{serviceId}/start:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: serviceId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response17"
      summary: Start
      tags:
      - Workspace Service Controller
  /api/workspaces/{id}/services/{serviceId}/stop:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: serviceId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response16"
      summary: Stop
      tags:
      - Workspace Service Controller
  /api/workspaces/{id}/stop-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
      summary: Stop Container
      tags:
      - Workspace Controller
info:
  title: qits-workspaces API
  version: 1.0.0-SNAPSHOT

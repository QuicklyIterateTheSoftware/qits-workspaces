A repository with no build manifests at all.

Integrating it is still a release: the version is computed from the clock regardless of stack, the
`release($version): …` merge commit is written, and no file is edited. The commit is the release;
the files are one stack's rendering of it.
